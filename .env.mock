# =============================================================================
# .env.mock — Overrides for using the fake OIDC server locally
# =============================================================================

# Point Okta env vars at the local fake OIDC server
OKTA_DOMAIN=http://localhost:9980
OKTA_CLIENT_ID=fake-client-id
OKTA_CLIENT_SECRET=fake-client-secret

# Disable shared auth (not needed locally)
ENABLE_SHARED_AUTH=false
